// this file should have all the API calls and put them into an object, that is 
    // then passed around with context or params into the places that it's needed

    function APICall() {
        // make api calls, useState the object
return (
    {
        // API Call results object
    }
)
    }

    export default APICall